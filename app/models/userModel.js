{/*
const mongoose = require('mongoose');

const usersSchema = {
    first_name: String,
    last_name: String,
    role: String,
    userId: Int32Array,
    PIN: Int32Array
}

const User = mongoose.model('Users', usersSchema);

module.exports = User;
*/}